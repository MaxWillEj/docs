# Logo Files

Add your logo files here:

- `dark.svg` - Logo for dark mode
- `light.svg` - Logo for light mode

Both should be SVG format for best quality at all sizes.

Recommended dimensions: 120x40 pixels (3:1 aspect ratio)

Update the paths in `docs.json` if you use different filenames.
